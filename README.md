# Tom Conlon ? standalone HTML copy

Open `index.html` directly in a browser. No installation, build step, server, JavaScript, or libraries are required. Upload this whole folder to any static web host to publish it.

- `index.html`: all page content and links.
- `styles.css`: styling, responsive layouts, local fonts, and marquee animation.
- `assets/`: images, fonts, favicon, and downloadable biographies.

The original React project is unchanged. Existing placeholder links (`href="#"`) are preserved; replace them with the real article, podcast, and social URLs when available. Keep the assets folder next to the HTML and CSS so relative links continue to work.
